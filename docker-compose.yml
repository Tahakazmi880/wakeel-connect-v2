services:
  postgres:
    image: postgres:16-alpine
    container_name: wakeel-connect-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: wakeel
      POSTGRES_PASSWORD: wakeel_dev_only
      POSTGRES_DB: wakeelconnect
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wakeel"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
